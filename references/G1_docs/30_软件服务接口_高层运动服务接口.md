# 高层运动服务接口

Source: <https://support.unitree.com/home/zh/G1_developer/sport_services_interface>

# 高层运动服务接口

高层运动服务接口的主要功能为机器人整机控制和上肢控制。整机控制可以控制机器人的模式切换与移动，提供 RPC 接口；上肢控制可以在运行机器人内置移动控制器的同时，单独控制上肢电机完成操作任务，提供 DDS 接口。

注意：

高层运动服务依赖于内置运控，进入调试模式后内置运控完全退出，高层运动服务失效。

# RPC接口

## 调用方式

用户可以创建 unitree::robot::g1::LocoClient 对象，通过成员函数发送请求，内置的高层运动服务响应请求，执行操作。

```
#include <unitree/robot/g1/loco/g1_loco_client.hpp>
#include <unitree/robot/g1/loco/g1_loco_api.hpp>

unitree::robot::g1::LocoClient client;
```

## 接口介绍

### 普通接口

| 函数名 | Damp |
| --- | --- |
| 函数原型 | int32\_t Damp() |
| 功能概述 | 机器人进入阻尼模式。 |
| 参数 | 无。 |
| 返回值 | 调用成功返回  0，否则返回相关错误码。 |
| 备注 | 最终状态切换是否成功取决于内置运控的状态切换逻辑，请参考前文《模式切换》。 |

| 函数名 | Start |
| --- | --- |
| 函数原型 | int32\_t Start() |
| 功能概述 | 通过模式切换，进入主运控。 |
| 参数 | 无。 |
| 返回值 | 调用成功返回  0，否则返回相关错误码。 |
| 备注 | 最终状态切换是否成功取决于内置运控的状态切换逻辑，请参考前文《模式切换》。 |

| 函数名 | Squat |
| --- | --- |
| 函数原型 | int32\_t Squat() |
| 功能概述 | 通过模式切换，进入下蹲模式。 |
| 参数 | 无。 |
| 返回值 | 调用成功返回  0，否则返回相关错误码。 |
| 备注 | 最终状态切换是否成功取决于内置运控的状态切换逻辑，请参考前文《模式切换》。 |

| 函数名 | Sit |
| --- | --- |
| 函数原型 | int32\_t Sit() |
| 功能概述 | 通过模式切换，进入落座模式。 |
| 参数 | 无。 |
| 返回值 | 调用成功返回  0，否则返回相关错误码。 |
| 备注 | 最终状态切换是否成功取决于内置运控的状态切换逻辑，请参考前文《模式切换》。 |

| 函数名 | StandUp |
| --- | --- |
| 函数原型 | int32\_t StandUp() |
| 功能概述 | 通过模式切换，进入站立模式。 |
| 参数 | 无。 |
| 返回值 | 调用成功返回  0，否则返回相关错误码。 |
| 备注 | 最终状态切换是否成功取决于内置运控的状态切换逻辑，请参考前文《模式切换》。 |

| 函数名 | ZeroTorque |
| --- | --- |
| 函数原型 | int32\_t ZeroTorque() |
| 功能概述 | 通过模式切换，进入零力矩模式。 |
| 参数 | 无。 |
| 返回值 | 调用成功返回  0，否则返回相关错误码。 |
| 备注 | 最终状态切换是否成功取决于内置运控的状态切换逻辑，请参考前文《模式切换》。 |

| 函数名 | Move |
| --- | --- |
| 函数原型 | int32\_t Move(float vx, float vy, float vyaw) |
| 功能概述 | 发送速度指令。 |
| 参数 | vx: 前进速度； vy: 水平速度； vyaw：旋转速度。 |
| 返回值 | 调用成功返回  0，否则返回相关错误码。 |
| 备注 | 速度指令默认生效 1 秒，可通过ContinuousGait函数修改行为。 |

| 函数名 | Move |
| --- | --- |
| 函数原型 | int32\_t Move(float vx, float vy, float vyaw, bool continous\_move) |
| 功能概述 | 发送速度指令。 |
| 参数 | vx: 前进速度； vy: 水平速度； vyaw：旋转速度； continous\_move：是否持续生效。 |
| 返回值 | 调用成功返回  0，否则返回相关错误码。 |
| 备注 | continous\_move=true时，速度指令长期生效；反之仅生效1秒。 |

| 函数名 | StopMove |
| --- | --- |
| 函数原型 | int32\_t StopMove() |
| 功能概述 | 将速度指令置零。 |
| 参数 | 无。 |
| 返回值 | 调用成功返回  0，否则返回相关错误码。 |
| 备注 | 无。 |

| 函数名 | SwitchMoveMode |
| --- | --- |
| 函数原型 | int32\_t ContinuousGait(bool flag) |
| 功能概述 | 修改 Move(float vx, float vy, float vyaw)的行为。 |
| 参数 | flag：设置 true 开启Move()持续响应模式，在运动模式下，会一直响应最新的Move指令。false 为关闭Move()持续响应模式，如果未收到新的Move指令，会延迟1s后自动停止。 |
| 返回值 | 调用成功返回  0，否则返回相关错误码。 |
| 备注 | 默认为关闭持续响应模式，为了安全，建议谨慎开启持续响应模式。 |

| 函数名 | BalanceStand |
| --- | --- |
| 函数原型 | int32\_t BalanceStand() |
| 功能概述 | 进入主运控的平衡站立模式。 |
| 参数 | 无。 |
| 返回值 | 调用成功返回  0，否则返回相关错误码。 |
| 备注 | 平衡站立模式下，速度指令为 0 且机器人平稳时，机器人停止踏步，仅在必要时踏步。 |

| 函数名 | ContinuousGait |
| --- | --- |
| 函数原型 | int32\_t ContinuousGait(bool flag) |
| 功能概述 | 持续移动。 |
| 参数 | flag：设置 true 为开启，false 为关闭。 |
| 返回值 | 调用成功返回  0，否则返回相关错误码。 |
| 备注 | 启动连续步态后，机器人会不断保持踏步状态，即使当前速度为 0。 |

### 专家接口

模式ID说明

| ID | 模式 | 备注 |
| --- | --- | --- |
| 0 | 零力矩 | 无平衡控制 |
| 1 | 阻尼 | 无平衡控制 |
| 2 | 位控下蹲 | 无平衡控制 |
| 3 | 位控落座 | 无平衡控制 |
| 4 | 锁定站立 | 无平衡控制 |
| 706 | 平衡下蹲、蹲起 | \ |
| 702 | 躺起 | \ |
| 500 | 常规运控 | \ |
| 501 | 常规运控-3Dof-waist | \ |
| 801 | 走跑运控 | \ |

| 函数名 | GetFsmId |
| --- | --- |
| 函数原型 | int32\_t GetFsmId(int& fsm\_id) |
| 功能概述 | 获取运控所在模式。 |
| 参数 | fsm\_id：用于接收当前模式id。 |
| 返回值 | 调用成功返回  0，否则返回相关错误码。 |
| 备注 | 详见模式ID说明。 |

| 函数名 | SetFsmId |
| --- | --- |
| 函数原型 | int32\_t SetFsmId(int fsm\_id) |
| 功能概述 | 设置运控模式。 |
| 参数 | fsm\_id：目标模式id。 |
| 返回值 | 调用成功返回  0，否则返回相关错误码。 |
| 备注 | 详见模式ID说明以及模式切换说明。 |

| 函数名 | GetFsmMode |
| --- | --- |
| 函数原型 | int32\_t GetFsmMode(int& fsm\_mode) |
| 功能概述 | 获取主运控是否处于站立状态。 |
| 参数 | fsm\_mode：用于接收状态。 |
| 返回值 | 调用成功返回  0，否则返回相关错误码。 |
| 备注 | 0：站立状态；1：移动状态。 |

| 函数名 | SetVelocity |
| --- | --- |
| 函数原型 | int32\_t SetVelocity(float vx, float vy, float omega, float duration = 1.f) |
| 功能概述 | 设置速度。 |
| 参数 | vx: 前进速度；vy：水平速度；omega: 旋转速度；duration：速度指令持续时间。 |
| 返回值 | 调用成功返回  0，否则返回相关错误码。 |
| 备注 | 程序会自动设置裁剪到允许范围。 |

| 函数名 | SetSpeedMode |
| --- | --- |
| 函数原型 | int32\_t SetSpeedMode(int speed\_mode) |
| 功能概述 | 走跑模式下调整最高速度。 |
| 参数 | speed\_mode: 速度模式，0:1.0m/s;1:2.0m/s;2:2.7m/s;3:3.0m/s |
| 返回值 | 调用成功返回  0，否则返回相关错误码。 |
| 备注 | 无 |

# DDS 接口

DDS 接口支持上肢控制，仅能在锁定站立、运控 1 与运控 2 中使用。

## 调用方式

用户可以根据[《DDS 通信接口》](https://support.unitree.com/home/zh/G1_developer/dds_services_interface)与底层服务[《底层服务接口》](https://support.unitree.com/home/zh/G1_developer/basic_services_interface)，向 rt/arm\_sdk 话题发送 LowCmd 类型的消息。参照[《关节电机顺序》](https://support.unitree.com/home/zh/G1_developer/joint_motor_sequence)，为上肢设置电机指令。

## 接口介绍

下表为 `LowCmd_.motor_cmd_`中每个有效元素的含义，其余元素均无效。

| 索引 | 含义 |
| --- | --- |
| 29 | MotorCmd\_.q 表示权重，取值范围为 [0.0, 1.0]。 |
| 12 - 28 | 腰部与上肢电机控制参数。 |
